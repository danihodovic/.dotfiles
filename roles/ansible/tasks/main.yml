---
- name: Install pip
  become: true
  apt:
    name: python-pip

- name: Install pip dependencies
  pip:
    name: [jmespath, boto]

- name: Install ansible
  pip:
    name: ansible
    version: '{{ version }}'
